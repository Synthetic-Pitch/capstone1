type SizeType = {
    className?:string;
}
const Tablet = ({className}:SizeType) => {
    return (
        <div className={className}>
            asdasd
        </div>
    );
};

export default Tablet;